# S6 wording repair return

The prescribed wording repair is complete. Writer source, build, render, and custody diagnostics pass.
Root acceptance and any principal integration remain outstanding.

## Candidate boundary

- Worktree: `/Users/raeez/mathematics/worktrees/frontier-resume-s6-wording-020-20260914`.
- Branch: `intake/resume-s6-wording-020-20260914`.
- Principal base: `5d9f632b5d9795e51075a9c15e2ee58d4cfb40af`, observed on clean `main` before worktree creation.
- Owned trees: `research-candidates/s6-marked-filling020/` and `reports/research/CONSTRUCTION-2026-09-14/s6-marked-filling020/`.
- Predecessor manifest: `2a8df4bdac7cc8f916e055c02a083fff26fd2175f5ab7da5a5e2288962242a0b`.
- New exact manifest: `candidate-manifest.json`. Its self-excluded SHA-256 is reported in the external final return.

Both destination trees were absent at the principal base. Each imported source was therefore a source-only file at a new assigned path.
The composition record binds each old source, new source, preserved baseline, and original hash.
No overlapping principal or predecessor file was replaced. No native principal integration occurred.

## Exact reader delta

Standalone `marked-comparison.tex:129` and native `marked-filling.tex:760` now read:

> These formulas specify the maps in every degree.

This replaces the predecessor sentence containing “requested adjacent degrees.” The line count is unchanged.
The exact byte comparison permits only those two replacements. Every other closure byte is preserved.
All 50 complete proof environments and all label, ref, and eqref commands remain byte-identical.
The native local source remains exactly the expanded standalone body after the unchanged label namespace operation.

Both full fresh review returns are preserved. They bind the original manifest and record bounded mathematical PASS under explicit reduction hypotheses.
Their sole required artifact correction is the sentence above. This return does not convert them into verdicts on the new manifest.

## Build and render evidence

All four fresh builds converged in four passes, including stable reference and PDF hashes.
The rebuilt baselines reproduce both original PDFs byte-for-byte.

- Standalone candidate: 17 pages, PDF SHA-256 `2198759c18d02c72728d943891e25cf7a9bffddcfe95f8e63acb976be02ab418`.
- Native comparison candidate: 45 pages, PDF SHA-256 `371b51d557d0096d17140f92d85f52aad036c34bfa56d458a54bc80050dcd637`.
- Source archive SHA-256: `cb4d1fc3dfe4ddfa3c68a085e8cbf65ae9f23ce51410b275e3649ea3e91aad45`.
- New-source patch SHA-256: `eb92454301571100928bc8d321a5035f2095ff4a99a67d62508caf3a616fdcf9`.
- Unapplied native patch SHA-256: `f286c45c3b3d8581a2cfdd83031dcec4429f39c381c6dbe1d98dc7533a7c8925`.

The complete auxiliary files are unchanged. Both page counts and every resolved label retain their prior values.
The normalized PDF text differs by exactly the prescribed sentence in each reader.
Whole-page raster comparison changes only standalone page 12 and native page 39.
All 60 other pages are PNG-byte-identical to the rebuilt baseline at 110 dpi.
The affected pages, their preceding pages, and their following pages were inspected visually.
There is no clipping, overlap, shifted subsequent content, or broken transition in this comparison.

The source, full PDF text, and PDF metadata scans pass the recorded firewall patterns.
The exact live style hash remains `07336dc2503195a619a5b566e8730e891d4c81b2465530873214d7087b83e4b5`.
Each source retains its approved shared-style symlink. Archived style bytes are custody snapshots only.
The existing build advisories remain unchanged and are recorded in `build-comparison.json`.

`build-inputs.json` binds every recorder input for all four builds. `commands-and-versions.json` supplies reproducible commands and installed versions.
`verify.py` rechecks the source delta, complete proof bodies, native expansion, old inputs, archive members, PDF text, page counts, rasters, and manifest.

## Custody and residual work

All 170 original manifest entries verified before import. All 171 files under the two original input scopes were archived unchanged.
All 360 inherited preserved-input records also verify. The original 002 tree and its complete native comparison remain untouched.
Both patches passed read-only applicability checks. The native patch's hunks reconstruct all three target files exactly from preserved native018 sources.
No files were staged or committed. No push or principal manuscript write occurred.

Two diagnostic implementation issues were corrected before freeze and are retained in `diagnostic-note.json`.
The patch tool initially removed a terminal blank line from each edited source. The exact delta assertion found this, and the original newline was restored.
The first namespace diagnostic excluded one leading body newline. Correcting that diagnostic boundary established exact equality without changing sources.
All final checks pass.

The mathematical scope remains the reviewed marked order-three local comparison under its explicit smooth reduction hypotheses.
The smooth reduction and commuting lift remain prerequisites. Other finite-order and cusp fillings, common-chain comparisons, and global gluing remain open obligations.
No new proof claim or sphere conclusion is made.

The remaining action is root review and acceptance of this frozen wording candidate, followed by any separately authorized integration and central PDF custody.
